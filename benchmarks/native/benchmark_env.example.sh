#!/usr/bin/env bash
export NATIVE_KECCAK_RPC_URL="${NATIVE_KECCAK_RPC_URL:-http://127.0.0.1:8545}"
export NATIVE_POSEIDON2_RPC_URL="${NATIVE_POSEIDON2_RPC_URL:-http://127.0.0.1:8546}"
export NATIVE_KECCAK_DATABASE_ID="${NATIVE_KECCAK_DATABASE_ID:-}"
export NATIVE_POSEIDON2_DATABASE_ID="${NATIVE_POSEIDON2_DATABASE_ID:-}"
export NATIVE_BLOCK="${NATIVE_BLOCK:-0x9}"
export NATIVE_ADDRESS="${NATIVE_ADDRESS:-}"
export NATIVE_WARMUPS="${NATIVE_WARMUPS:-3}"
export NATIVE_REPEATS="${NATIVE_REPEATS:-10}"
